org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Portable JDK 17 for the Android build (machine default java is 8, too old for AGP 9).
org.gradle.java.home=D:/app/java/jdk17
android.useAndroidX=true
# Kotlin incremental compilation locks .tab cache files on Windows (Defender),
# causing "Could not close incremental caches". Disable it for a reliable build.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
